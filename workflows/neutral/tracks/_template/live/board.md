# live/board.md — cross-session board

> Append-only rows, newest at the bottom. Re-read the file before appending (never rewrite a row).
> Row ID = your session + sequence (`#<sid>-<n>`). Single-session work adds NO rows.

| ID | Who | Type (request / handoff / notice / incident) | Task ref | Message | Status |
|---|---|---|---|---|---|
| #sess-main-1 | sess-main | notice | 2.4 | Wave 2.4 otw — verify lock held until 14:00 | open |
| … | | | | | |