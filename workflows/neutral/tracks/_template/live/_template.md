# sess-<sid>

- identity: <tool> (model: <verified modelID, e.g. via your tool's debug/about screen — resolved
  date> | unresolved) — **verify fresh each session; never copy from older docs**
- started: <yyyy-MM-dd> · state: active · **heartbeat: <yyyy-MM-dd HH:mm>** (stamp every turn via
  coordination.ps1 -Heartbeat — a heartbeat older than 12h voids your claims)
- scope: <your zones from zones.md>

## Current task

<plan task id + verb>

## Touched files

- <path> — <what>
- (keep this register current — the staging gate overlaps it against other sessions)

## Next

<task id — the continuation line for the next session (even after compaction)>