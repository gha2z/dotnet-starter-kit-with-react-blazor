# Zones — <track-name>

> **The ownership map the coordination gate parses.**
> Format: one "owner: <session-prefix>" line per zone.
> "#" comments allowed. Owner is the session id without the "sess-" prefix.
> Add new zones as you add modules / pages / components.

# Zones map — one owner per line.
# Add new zones as you add modules / pages / components.
# Format: owner: <session-prefix>

blazor: main
