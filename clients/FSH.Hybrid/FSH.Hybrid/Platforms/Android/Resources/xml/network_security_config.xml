<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted ONLY for the Android-emulator host-loopback aliases
  (10.0.2.2 / localhost / 127.0.0.1) used in local development against a
  dev API (http://10.0.2.2:5030). Everything else stays HTTPS-only, and the
  production network security default (cleartext blocked) is preserved.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
