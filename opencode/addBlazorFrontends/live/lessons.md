@ lessons.md — session lesson ledger (append-only, rule 13).
# Read the tail at every turn start; append exactly one line at every turn end. Each line: <timestamp> sess-<id> :: <failure -> root cause -> remedy -> proof>. The Gate refuses to pass while this file is missing.
# Failure-pattern registry (RECURRING classes of failure — when a new event matches one, append with tag [RECUR]):
#  [PATH-CLASS] node driver fails when script/evidence dirs aren't absolute paths (Path resolution bug in Toolbox) -> always run driver with absolute paths, workdir-independent
#  [EVAL-BLOCK] scripts that LOAD a model's eval system get blocked (empty-eval instrumentation trap) -> ship standalone logic without eval wiring; code is checked-in, not generated in-session
#  [BATCH-ESCALATION] parallel tool batches after failures escalate the PRM circuit breaker -> after any failure, drop to single sequential tool calls; never batch retries
#  [FAILED-CALL-MULTIPLY] a failed tool call + its retry both count toward escalation -> do not blindly retry; read the error, change the approach
- 2026-08-13 19:36 sess-main :: Phase-10 run triage: react list pages captured pre-hydration (int=17, h1=0 = sidebar chrome only) because anchors match sidebar nav text -> fix waitSettled to anchor-scope to <main> first, fallback with extra settle delay
- 2026-08-13 19:36 sess-main :: blazor headings are div-based (FshPageHeader, h1=0) vs react real h1 -> parity heading rule must flag ASYMMETRY only, never both-zero
- 2026-08-13 19:37 sess-main :: react A13 invoice-detail "no data rows" was FALSE — rows are divs (SUB-202608-ACME present, $290.00) not <tr> -> registry rowSelectorReact div.cursor-pointer + add div/role=row "rowLike" count to snapshots so parity sees div-rows
- 2026-08-13 19:40 sess-main :: react apps log 1 console error on every page = SignalR hub negotiation failure (pre-existing, react-side) -> treat as finding, not gap; parity console-error comparison must exclude hub negotiation
- 2026-08-13 19:42 sess-main :: PRM escalation fired twice on batch patterns (read+grep parallel after 3-failure breaker persists across turns) -> after ANY failure, strictly single tool calls; batch only when every member is a fresh success-class call- 2026-08-15 14:26 sess-main :: The canonical gate's TrackRoot default already points at the existing live track (opencode/addBlazorFrontends) — canonicalize by dropping zones.md INTO the track (v1 hardcoded map → zones.md authority), never by moving the track. Root cause: v2 split zones into a file; the repo's real sessions live in the legacy dir. Remedy: convert in place (zones.md), keep legacy files untouched. Proof: -Start passed against the live track with zones.md parsed.
- 2026-08-15 15:10 sess-main :: When editing install-script sections in markdown, keep the comment lines INSIDE the bash code fence; a stray closing fence without its opener turns the rest of the document into one code block. Remedy: fence as one unit and re-read the rendered region. Proof: neutral README fences now balanced, rest of file renders.
- 2026-08-17 17:35 sess-main :: D31 branding interact first run: react inputs locator input[type="text"] matched 0/39 - react shadcn Input renders NO type attribute -> use input[type="color"] for PaletteInput and input:not([type]) for text fields; getByRole blocked by aria-labels (Primary colour etc). Remedy: attribute-based locators, and cross-check full-render debug counts before asserting. Proof: d31-branding-interact.mjs ALL CHECKS PASSED (save/reload/reset parity both apps).
