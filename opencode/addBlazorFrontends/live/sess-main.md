# sess-main
identity: opencode/deepseek-v4-flash-free (concrete modelID, verified 2026-08-06) | started: 2026-08-06 | state: active | heartbeat: 2026-08-11 13:04

scope: clients/dashboard-blazor/** · clients/BlazorShared/** (Main-only) · clients/admin-blazor/** (Main-owned by default; app-code edits by other sessions need a board sign-off per task) · STATUS.md · 00-Index.md · Phase-02/03/04/06/07 plan files · opencode/addBlazorFrontends coordination docs (readme.md, verify.ps1, coordination.ps1) · implementation summaries

## Current task
Workflow enhancement (waves 20–21b, user-approved): **COMPLETE — waves 21+21b committed `9e95132b`** (coordination.ps1 zone map +`AGENTS.md`/`.agents/workflows/`/dir-wide `opencode/addBlazorFrontends/`, readme.md Scope Restriction + ritual step 8 + board row #10, NEW WORKFLOW-GUIDE.md task→skill map, AGENTS.md enhanced-workflow bullet, feature-scaffolder/code-reviewer plugin-skill refs). Wave-20 leftovers (00-Index.md + waves 16–19 summary) committed in same. **Q2 DONE**: global opencode.jsonc +13 dotnet skill paths (convert-to-cpm, 6× dotnet-upgrade, 6× template-engine) — node-validated. **Q2b DONE**: plugin array = opencode-swarm + @cortexkit/opencode-magic-context@latest + @atonev/opencode-short-term-memory@latest + opencode-handoff + ./plugins/opencode-review.ts (local clone sun-praise/opencode-review, symlinked); compaction auto/prune OFF (magic-context manages context); magic-context.jsonc + .opencode/stm.jsonc written. Verify: skill load-test OK (run-tests resolves), gate PASSED, tree clean. **Restart opencode to load new plugins.**
M2 (Phase 6 polish/perf) — **COMPLETE (all 6.x code items done, waves 1–16 committed)**, Phase 7 gaps at zero; **wave 18 COMPLETE (6.9 re-test on SDK 10.0.302 — artifact documented); wave 19 COMPLETE — 6.9 RESOLVED via clean-publish re-test**.
- Waves 1-3 committed (`e5bb0367` trim/splash/a11y, `6b6fa334` publish smoke/AOT-off/deployability, `a9c78f86` READMEs + slow-network E2E).
- **Wave 4 committed (`5d4eaaca`): dashboard lazy loading** — `Pages/` + `TerminalLayout.razor` → RCL `FSH.Dashboard.Pages`. E2E 18/18 + bUnit 179/179 green. 20.34 MB / 4.57 gz (Pages deferred, 645.8 KB lazy file).
- **Wave 5 committed (`8bc78500`): admin lazy loading** — `Pages/**` (77 files) → RCL `FSH.Admin.Pages`. bUnit **158/158 green**. Trimmed publish: lazyAssembly confirmed, 21.17 MB total.
- **Wave 6 committed (`68401ba3`): PWA both apps** — manifest.json (rose theme), service-worker.js (v1.0, cache-first `_framework/*`, offline.html fallback), offline.html, 192/512/maskable PNG icons. Both apps build 0 errors; admin 158/158 + dashboard 179/179 green; publish smoke confirms PWA assets emitted.
- **Wave 7 committed (`c8b0624f`): 6.4 contrast/focus DONE** — WCAG AA audit → Light Primary `#D11A42` (was #E11D48, 4.31 on bg), Secondary `#457383` (was 4A7B8C, 4.28), Dark PrimaryContrastText `#121216` (white was 2.69 on #FB7185); global `:focus-visible` in fsh.css. Guard: `FshMudThemeContrastTests` (2 new, dashboard 181/181 + admin 158/158).
- **Wave 9 committed (`0cbbfde3`): 6.7 docs DONE (main zone)** — root README: Blazor WASM run commands + ports (5175/5176) + MAUI run note + repo-layout rows point at the migration guide (board row #5 announcement first — shared zone). New `clients/BlazorShared/MIGRATION-GUIDE.md`: React→Blazor concept map, per-page checklist, conventions, real-time/offline parity. MAUI README row stays sess-maui (idle).
- **Wave 10 committed (`11c8c3c8`): 6.8 audit + fix** — memory-leak audit — all 17 `+=` subscription sites have matching `-=` in Dispose (verified); `InactivityTimerService` IAsyncDisposable; ChatPage disposes SignalR subs. Edge cases: empty lists (FshEmptyState ×39), long text (fsh-truncate + title ×55), XSS (zero MarkupString in clients/), multi-tab logout (storage events + E2E). **Real bug found + fixed**: `AuthDelegatingHandler` refresh-token rotation race — read refresh token before the lock, second concurrent 401 refreshed with rotated (invalid) token → session killed. Fix: re-read store after lock (double-checked). 4 new `AuthDelegatingHandlerTests` → dashboard **200/200**, admin **158/158**; also fixed pre-existing CS0105 duplicate using in admin LoginPage.razor.
- **Wave 11 committed (`c2a67605`): 6.2 chat infinite scroll** — scroll-top load-older. New `clients/BlazorShared/wwwroot/js/fshChatScroll.js` ES module (watchScrollTop/unwatchScrollTop with per-ref cleanup map, scrollToBottom, scrollHeight, restoreScrollPosition). ChatPage.razor.cs: cursor `before: _messages[0].Id` via `ListChannelMessagesAsync` (InitialPageSize=100, OlderPageSize=50), `_hasOlder` from full-page return, dedupe vs SignalR arrivals, scroll offset preserved across prepend, `[JSInvokable] OnScrollTopReached` + `_loadingOlder` guard, module invoked via `IJSObjectReference` (never global lookup — module exports are not globals). Activity page: N/A (live SSE ring buffer, no history endpoint — parity with React). 4 new ChatPageTests → dashboard **204/204**. bUnit gotchas: NSubstitute `.Returns(value, Task)` mix rejected — use `_ =>` func form; gate-TCS tests must not await the gated InvokeAsync.
- **Wave 12 committed (`a0c83aa9`): 6.3 preload + 6.4 a11y** — boot-JS preload (`_framework/blazor.webassembly.js`, `_framework/dotnet.js` — stable names) + font preconnect (googleapis + gstatic crossorigin) in both apps' index.html. Assembly preload impossible (content-hashed `MudBlazor.xr72q1v0gr.wasm`); API preconnect skipped (runtime config.json). 6.2 image-lazy verified already in code (all `<img loading="lazy">`). 6.4 SR/form/focus/MudBlazor a11y audit DONE — **found MudBlazor 9.7 does NOT emit `role="alert"` on MudAlert** → added to FshOfflineBanner (+guard assertion in FshOfflineBannerTests: role=alert + icon aria-hidden); chat composer `aria-label="Message"`; MudIcon aria-hidden default verified; MudDialog role/dialog native; tables documented (h1-named regions); labeled MudTextFields render proper for/id. Dashboard **204/204**, admin **158/158** (re-verified post-commit).
- **Wave 13 committed (`9ad1b745`): 6.2 render optimization** — `@key` added to 4 reorder-sensitive lists — ChatPage messages (`msg.Id`, wave-11 prepend shifts every position) + channels (`ch.Id`), SessionsPage (`session.Id`, sorted by LastActivityAt), NotificationsInboxPage (`notif.Id`, SignalR push `Insert(0,…)`). Stable paginated lists deliberately NOT keyed (no reorder — no diffing win). StateHasChanged audit: only 2 sites in both apps, both event-driven (Overview SSE state, WebhookCreateDialog draft). ShouldRender override: audited N/A (no timer/hot-loop renders) — documented, no code. Dashboard **204/204**, admin **158/158**.
- **Wave 14 committed (`baff033f`): 6.3 critical CSS + HTTP/2 push decision** — splash styles (`.fsh-loading-*` + `fsh-spin`) inlined from `css/app.css` into a `<style>` block in both index.html — splash is the ONLY above-the-fold content pre-boot, so first paint no longer waits on the app.css request. Dashboard app.css trimmed to post-boot rules (skip-link, route-loading); admin app.css was 100% splash → inlined + file/link deleted. **MudBlazor deferral rejected**: CSS fetches in parallel with the ~20MB runtime download (real boot bottleneck); arrives before first interactive render; `media="print"`-trick would only risk FOUC. Fonts left as render-blocking link (`display=swap` + preconnect = effectively non-blocking). **HTTP/2 Server Push: superseded tech (removed Chrome 106+, never in Firefox) — documented 103 Early Hints guidance for the deployment zone instead; preload (wave 12) covers the same win cache-aware.** Both apps build 0 errors; dashboard **204/204**, admin **158/158**.
- **Wave 16 committed (`be50e8ca`): coordination.ps1 gate fix + theme seam + Phase 6/7 doc sync** — **gate bug fixed**: `git status --porcelain` unstaged lines (` M path`) fell through all regexes → unstaged changes outside zones silently PASSED the gate (fail-closed intent bypassed). Now parse the XY two-column form (covers ` M`, `M `, `MM`, `AM`, `R old -> new`, `T`/`U`) in both the ownership check and the overlap check; rename lines check the worktree target. Gate regression test verified via synthetic lines (`.opencode/temp/gate-regex-test.ps1`). Also: root `.gitignore` now excludes `.opencode/` (local AI-tool state; was failing the gate for every session) + zone map/Scope Restriction updated to match. **FshThemeService unsealed + persistence seams** (`ReadStoredAsync`/`WriteStoredAsync` protected virtual) so sess-maui's hybrid can back the same theme state with SecureStorage (plan B) — WASM behavior unchanged, dashboard **204/204** + admin **158/158** re-verified under lock. **Phase 6 plan: 🟢 complete** (6.1 tree-shaking/pre-compression verified, AOT decision ticked, 6.5 parity items ticked); **Phase 7 plan: all app gaps at zero** (3.12 Settings 7 pages, 3.13 Terminal pages, 3.14 palette, styled 404 verified). **Incident (rule 12):** my wave-16 staged set was swept into sess-maui's commit (`2acbbf8c`) by the shared-index race (their `git commit` took the whole index) → soft-reset tip + re-committed cleanly as `0b40206d` (theirs) + `be50e8ca` (mine). Lesson: never stage while the other session may commit; stage+commit in one go.
- **Wave 18 (6.9 re-test, SDK 10.0.302):** Published dashboard WASM (`dotnet publish -c Release -o .opencode/temp/publish-618`). Root cause identified: **SDK 10.0.302 Release publish emits ONLY compressed `.br`/`.gz` sidecars for `_framework/*.js` — the uncompressed originals (`blazor.webassembly.js`, `dotnet.js`) are missing**. Native `.wasm` files DO have uncompressed copies, but the two critical boot `.js` files referenced by `index.html` do not. SPA-fallback static server returns `index.html` (200, text/html) for the missing paths → Playwright smoke: `Unexpected token '<'` ×5, `unsupported MIME type ('text/html')`, `blazor-error-ui=false`, `mud-theme-provider=0` — app never boots. **`blazor.boot.json` also completely absent** (no `.json` in `_framework/` at all — not even compressed). Boot config is inlined into `dotnet.js` per .NET 10 design (between `/*json-start*/`/`/*json-end*/`), but `dotnet.js` itself is missing uncompressed. **Diagnosis: SDK 10.0.302 publish-layout regression** — earlier waves' publishes (same csproj) emitted both uncompressed JS and boot.json; the new SDK apparently drops the uncompressed JS copies from the output while keeping only compressed sidecars. **This supersedes the old NIY crash** — we can't even reach the Mono interpreter because the app never loads the WASM runtime. Workarounds: (1) serve with `dotnet serve` or ASP.NET host that does content-negotiation on `.br`/`.gz`, (2) add `<BlazorWebAssemblyJSHostCompression>` property to control output, (3) pre-expand compressed files in the publish output. Production deployment requires a server that serves compressed variants with proper `Content-Encoding` headers.
- **Wave 19 (2026-08-09, 6.9 clean-publish re-test — WAVE-18 CONCLUSION REVERSED):** Repeated the Release publish **after deleting `obj/Release` + `bin/Release`** (dashboard → `.opencode/temp/publish-618b`, then a second clean pass → `publish-619c`; admin → `publish-admin-619`). **All three publishes DO contain the uncompressed `blazor.webassembly.js` + `dotnet.js`** (plus `.br`/`.gz` sidecars). `blazor.boot.json` absent by design (boot config inlined in `dotnet.js` — verified: `mainAssemblyName=FSH.Dashboard.Wasm`, 75 eager assemblies + `lazyAssembly=FSH.Dashboard.Pages.2vrzuz3ko9.wasm` present). **Boot smoke (plain HttpListener static server, SPA fallback, port 5190, Playwright): login page fully rendered in ~4.3 s — `theme:true`, `hasSignIn:true`, `errorUi:false`, 0 pageerrors, 0 failed requests, 170 framework requests OK.** **No Mono NIY crash** — the old #121849 blocker does not reproduce either. **Conclusion: wave-18's "missing uncompressed JS" was a stale-publish artifact** — reusing the existing `obj/Release` intermediates + partially-updated output folder yielded a publish output missing the uncompressed JS. The plan's measurement gotcha ("always clean `obj/Release` between configs") applies to the publish output folder too. **New rule for boot verification: clean publish (`obj/Release` deleted or fresh `-o`) and confirm uncompressed `blazor.webassembly.js` + `dotnet.js` exist in `_framework/` before smoke-testing.** **6.9 CLOSED** — no code change needed; the previously documented workarounds (content-negotiation, CDN `Content-Encoding`) remain valid deployment-zone guidance for the `.br`/`.gz` sidecars (6.1 pre-compression note), but are NOT required for boot.
Next: **6.9 RESOLVED (wave 19)** — remaining: deployment-zone (Early Hints, CDN compression config), cross-session (MAUI README), last-known-good cache (deferred). Watch board for sess-maui's A–D progress.
## Touched files (clean — waves 20–21b + Q2/Q2b committed `9e95132b`)
- none pending

## Notes for other sessions
- **sess-maui (plan B)**: `FshThemeService` (BlazorShared) is **unsealed** with `protected virtual ReadStoredAsync()` / `WriteStoredAsync()` seams — committed in `be50e8ca` (wave 16) — subclass it in FSH.Hybrid to persist via SecureStorage (same pattern as `MauiTokenStore`); base behavior (localStorage via fshTheme.js) unchanged. Do NOT touch BlazorShared itself.
- **Index-race lesson (rule 12, wave 16)**: a concurrent `git commit` from another session sweeps the whole shared index — my staged set landed inside sess-maui's `2acbbf8c`; fixed via soft-reset + re-commit (`0b40206d` theirs + `be50e8ca` mine). Stage+commit in one go; if a foreign commit lands mid-staging, expect index churn and re-verify.
- ✅ ~~**6.9 RELEASE-PUBLISH (wave-18 finding, SDK 10.0.302)**~~ **RESOLVED (wave 19)**: wave-18 claimed Release publish was missing the **uncompressed** `_framework/*.js` files (`blazor.webassembly.js`, `dotnet.js` — only `.br`/`.gz` sidecars remained) AND that `blazor.boot.json` was absent. **Clean re-publish (deleted `obj/Release`) proved this was a stale-publish artifact**: uncompressed JS IS emitted; boot config inlined in `dotnet.js` (no boot.json = .NET 10 design); **plain static host boots to login in ~4.3 s, zero errors, no Mono NIY crash** — on BOTH apps. 6.9 CLOSED, no code change. Rule: clean `obj/Release` (or fresh `-o`) before any publish-boot verification; confirm uncompressed `_framework/blazor.webassembly.js` + `dotnet.js` exist first. Deployment-zone CDN guidance (serve `.br`/`.gz` with `Content-Encoding` per 6.1 pre-compression) still stands but is not required for boot.
- **`OverrideHtmlAssetPlaceholders=true` breaks static hosting** — do NOT re-add (Phase-06 plan gotchas).
- verify.lock: free as of 08:35 (Main holds it during wave-18 builds — check before any dotnet build, esp. sess-maui plan C SQLite bump).
- On the protocol as of 2026-08-06 03:26 (identity re-verified, heartbeat live).
- Last landed commits: e5bb0367 (wave 1) + 6b6fa334 (wave 2) + a9c78f86 (wave 3) + 70dd9af2 (hybrid fix) + 78606f20/5352e3d7 (sess-maui hybrid) + 5d4eaaca (wave 4) + ae20d12d (board row #3) + 8bc78500 (wave 5) + 68401ba3 (wave 6) + c8b0624f (wave 7) + 123d7517 (wave 8) + 0cbbfde3 (wave 9) + 11c8c3c8 (wave 10) + c2a67605 (wave 11) + a0c83aa9 (wave 12) + 9ad1b745 (wave 13) + baff033f (wave 14) + f7acfc16 (wave 15) + 5045e07c/3b12a2fc/d6e54783 (sess-maui hybrid/seed/verify).

## Blockers / requests to other sessions
- [x] ~~**6.9**~~ **CLOSED (wave 19)**: clean-publish re-test reversed the wave-18 finding — Release publish boots on plain static host, no NIY crash. See wave-19 note.
- [x] 3.14 landed + gate green → Phase C admin palette UNPARKED.
- [x] Board row #2: accent/font/density sign-off posted for sess-maui.
- [x] Board row #3: Phase-07 MAUI gap table refreshed (ae20d12d).
